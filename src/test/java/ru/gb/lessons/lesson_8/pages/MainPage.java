package ru.gb.lessons.lesson_8.pages;

import org.openqa.selenium.WebDriver;

public class MainPage extends BasePage {
    public MainPage(WebDriver webDriver) {
        super(webDriver);

//    WebDriverRunner.getWebDriver();                 //получаем драйвер селенида (он статический для получения доступа из любого класса)
//   $()                                              // вместо файндЭлемента в силиниде используют $

    }

}